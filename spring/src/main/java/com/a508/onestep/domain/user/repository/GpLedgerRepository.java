package com.a508.onestep.domain.user.repository;

import com.a508.onestep.domain.user.entity.GpLedger;
import org.springframework.data.jpa.repository.JpaRepository;

public interface GpLedgerRepository extends JpaRepository<GpLedger, Long> {
}
